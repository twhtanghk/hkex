// Generated by CoffeeScript 2.7.0
var HKEXList, HKEXNew, XLSBuffer, range, reverse, service;

import _ from 'lodash';

import http from 'needle';

import moment from 'moment';

import {
  Buffer
} from 'buffer';

import {
  Transform
} from 'stream';

import {
  read,
  utils
} from 'xlsx';

HKEXNew = (function() {
  class HKEXNew {
    async * iter() {
      var alert, i, j, res, results, type;
// hkex only allowed to fetch latest 5 pages of news alert in descending order
      results = [];
      for (i = j = 1; j <= 5; i = ++j) {
        res = (await http('get', HKEXNew.url({
          page: i
        })));
        results.push((yield* (function*() {
          var k, len, ref, results1;
          ref = res.body.newsInfoLst;
          results1 = [];
          for (k = 0, len = ref.length; k < len; k++) {
            alert = ref[k];
            type = alert.lTxt.split(' - ');
            results1.push((yield {
              releasedAt: moment(alert.relTime, 'DD-MM-YYYY HH:mm').toDate(),
              code: alert.stock[0].sc,
              name: alert.stock[0].sn,
              type: type[0],
              typeDetail: type[1],
              title: alert.title,
              link: alert.webPath,
              size: alert.size
            }));
          }
          return results1;
        })()));
      }
      return results;
    }

  };

  HKEXNew.url = _.template(process.env.URL || 'https://www1.hkexnews.hk/ncms/json/eds/lcisehk1relsdc_<%=page%>.json');

  return HKEXNew;

}).call(this);

reverse = async function*(iterator) {
  var done, value;
  ({value, done} = (await iterator.next()));
  if (!done) {
    yield* reverse(iterator);
  } else {
    return;
  }
  return (yield value);
};

range = function(sheet) {
  var cells, col, pattern, row;
  pattern = /([A-Z]+)([0-9]+)/;
  cells = _.filter(_.keys(sheet), function(key) {
    return pattern.test(key);
  });
  row = _.max(_.map(cells, function(key) {
    var ret;
    ret = key.match(pattern);
    return parseInt(ret[2]);
  }));
  col = _.max(_.map(cells, function(key) {
    var ret;
    ret = key.match(pattern);
    return ret[1];
  }));
  return `A4:${col}${row}`;
};

XLSBuffer = (function() {
  class XLSBuffer extends Transform {
    constructor(opts = {
        readableObjectMode: true,
        writableObjectMode: true
      }) {
      super(opts);
    }

    _transform(data, encoding, cb) {
      this.buffer.push(data);
      return cb();
    }

    end() {
      var Sheets, data, err, j, len, opts, ref, row;
      try {
        data = Buffer.concat(this.buffer);
        ({Sheets} = read(data, {
          type: 'buffer'
        }));
        opts = {
          range: range(Sheets.ListOfSecurities),
          header: ['code', 'name', 'category', 'sub-category', 'lot', 'value', 'isin', 'expiry date', 'stamp duty', 'shortsell eligible', 'cas eligible', 'vcm eligible', 'stock options', 'stock futures', 'ccass', 'etf', 'debt securities board lot', 'debt securities investor type']
        };
        ref = utils.sheet_to_json(Sheets.ListOfSecurities, opts);
        for (j = 0, len = ref.length; j < len; j++) {
          row = ref[j];
          this.push(row);
        }
        return this;
      } catch (error) {
        err = error;
        return this.emit('error', err);
      }
    }

  };

  XLSBuffer.prototype.buffer = [];

  return XLSBuffer;

}).call(this);

HKEXList = function() {
  return http.get(process.env.STOCKLIST || 'https://www.hkex.com.hk/chi/services/trading/securities/securitieslists/ListOfSecurities_c.xlsx').on('err', function(err) {
    throw err;
  }).pipe(new XLSBuffer());
};

service = {
  details: async function(code) {
    var url;
    url = process.env.STOCK_URL || `https://ttsoon.ml/hkexkoa/api/stock/${code}`;
    url = url.replace('#{code}', code);
    return ((await http('get', url))).body;
  },
  category: async function(code) {
    var details;
    details = (await service.details(code));
    return [details['category'], details['sub-category']];
  },
  name: async function(code) {
    return ((await service.details(code))).name;
  },
  isETF: async function(code) {
    return '交易所買賣產品' === ((await service.category(code)))[0];
  },
  isEquity: async function(code) {
    return '股本' === ((await service.category(code)))[0];
  }
};

export default {HKEXList, HKEXNew, reverse, service};

export {
  HKEXList,
  HKEXNew,
  reverse,
  service
};
